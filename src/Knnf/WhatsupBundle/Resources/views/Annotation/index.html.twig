{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Annotation list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Iduser</th>
                <th>Annotationtype</th>
                <th>Annotationcontent</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('annotation_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.idUser }}</td>
                <td>{{ entity.AnnotationType }}</td>
                <td>{{ entity.AnnotationContent }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('annotation_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('annotation_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('annotation_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
